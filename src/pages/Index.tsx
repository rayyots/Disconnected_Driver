
import React from 'react';
import HomePage from './HomePage';

const Index: React.FC = () => {
  // Directly render HomePage with no redirects or auth
  return <HomePage />;
};

export default Index;
